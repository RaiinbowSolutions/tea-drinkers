{
  "name": "tea-drinkers",
  "version": "1.0.1",
  "private": true,
  "description": "",
  "keywords": [
    "netlify",
    "express",
    "serverless"
  ],
  "author": "Raiinbow Solutions <raiinbowsolutions@outlook.com>",
  "license": "MIT",
  "scripts": {
    "start": "run-p start:**",
    "start:api": "npm run start --workspace=api --if-present",
    "start:client": "npm run start --workspace=client --if-present",
    "build": "run-p build:**",
    "build:api": "npm run build --workspace=api --if-present",
    "build:client": "npm run build --workspace=client --if-present",
    "test": "run-p test:**",
    "test:api": "npm run test --workspace=api --if-present",
    "test:client": "npm run test --workspace=client --if-present"
  },
  "dependencies": {
    "npm-run-all": "^4.1.5",
    "cross-env": "^7.0.3"
  },
  "workspaces": [
    "api",
    "client"
  ]
}
